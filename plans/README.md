# Implementation Plans

Generated by the improve skill on 2026-07-30. Execute in the order below.
Each executor must read the plan fully, honor its STOP conditions, and update
the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Extract the library directory picker model | P1 | M | — | DONE |

Status values: TODO, IN PROGRESS, DONE, BLOCKED (with a one-line reason), or
REJECTED (with a one-line rationale).

## Dependency notes

- Plan 001 is self-contained and depends on the fuzzy folder search at
  `b6aaf64`.

## Findings considered and rejected

- Rewrite the C codebase into object-oriented pseudo-C++: rejected because
  vtables, inheritance, and heap-only opaque objects do not address the
  demonstrated coupling.
- Split files solely to satisfy a line-count threshold: rejected because
  `reader_tui.c` is larger than `library_tui.c`, while the folder picker is the
  specific cohesive boundary with mixed ownership and testability costs.
- Refactor `reader_tui.c` in the same phase: rejected as unrelated and too
  risky for a behavior-preserving folder-picker extraction.
